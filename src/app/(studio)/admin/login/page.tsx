import LoginForm from './form/form.component';

const LoginPage = () => {
  // After user created redirect to login page.
  return (
    <div className='max-w-sm mx-auto'>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
